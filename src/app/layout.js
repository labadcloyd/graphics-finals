import "./globals.css";

export const metadata = {
  title: "Sketchpad - Midpoint Ellipse",
  description: "Sketchpad - Midpoint Ellipse",
};

export default function RootLayout({ children }) {
  return (
    <html lang='en'>
      <body>{children}</body>
    </html>
  );
}
